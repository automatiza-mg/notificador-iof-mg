# Configuração da Aplicação
APP_NAME=notificador-iof-mg
APP_ENV=development
CLIENT_URL=http://localhost:5173
DIARIOS_DIR=diarios

# Banco de Dados
# SQLite (MVP):
DATABASE_URL=sqlite:///local.db
# PostgreSQL (produção):
# DATABASE_URL=postgresql://user:password@localhost/dbname

# Redis (para RQ)
REDIS_URL=redis://localhost:6379/0

# Email
# Para desenvolvimento local, você pode usar MailHog (https://github.com/mailhog/MailHog)
# ou configurar um servidor SMTP real (Gmail, SendGrid, etc.)

# Configuração para Gmail (RECOMENDADO):
# IMPORTANTE: Para Gmail, você precisa criar uma "Senha de App" em:
# https://myaccount.google.com/apppasswords
# Use essa senha de app, NÃO sua senha normal do Gmail
MAIL_FROM_ADDRESS=pythontestaugusto@gmail.com
MAIL_SMTP_HOST=smtp.gmail.com
MAIL_SMTP_PORT=587
MAIL_USE_TLS=true
MAIL_USE_SSL=false
MAIL_SMTP_USER=pythontestaugusto@gmail.com
MAIL_SMTP_PASSWORD=sua-senha-de-app-aqui

# Para desenvolvimento local com MailHog:
# MAIL_FROM_ADDRESS=noreply@example.com
# MAIL_SMTP_HOST=localhost
# MAIL_SMTP_PORT=1025
# MAIL_USE_TLS=false
# MAIL_USE_SSL=false
# MAIL_SMTP_USER=
# MAIL_SMTP_PASSWORD=

# Exemplo para SendGrid:
# MAIL_SMTP_HOST=smtp.sendgrid.net
# MAIL_SMTP_PORT=587
# MAIL_USE_TLS=true
# MAIL_SMTP_USER=apikey
# MAIL_SMTP_PASSWORD=seu-api-key-sendgrid
